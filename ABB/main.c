#include <stdio.h>
#include <stdlib.h>
#include "TADArbolBin.h"

int main(int argc, char *argv[])
{
	ArbolBinario = arbol
	Inicializar(arbol);
	int a[3] = {1,2,3};
	
	for (int i = 0; i < 3; i++)
	{
		Insert(arbol,a[i]);
	}
	PreOrden(&arbol,Raiz(&arbol));
}
